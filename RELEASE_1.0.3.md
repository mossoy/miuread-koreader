# MiuRead v0.1.0-dev.15

## 下载目录选择修复

- 删除需要手写绝对路径的输入框。
- 改用 KOReader 原生 PathChooser 文件夹选择器。
- 仅显示文件夹，不显示普通文件。
- 可通过返回键直接退出选择器。
- 长按目标文件夹名称后确认选择。
- 支持在选择器中创建新文件夹。
- 保存前验证目录存在且可写。
- 设置菜单显示当前下载目录。
- 修改目录仅影响以后下载的书籍，既有文件不自动移动。
